import Form from "./Form";

import styled from "styled-components";

const FormWithBackground = styled(Form)`
  background-color: var(--color-grey-0);
  padding: 2rem 3rem;
`;

export default FormWithBackground;
